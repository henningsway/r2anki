---
title: "testrmd"
author: "Henning"
date: "27 Februar 2017"
output: 
  html_fragment:
  html_document:
    # theme: "simplex"
    highlight: "zenburn"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown
```{r}
library(knitr)
thm <- knit_theme$get("acid")

thm = knit_theme$get()  # parse the theme to a list
knit_theme$set("acid")

opts_knit$set(out.format = NULL)

set_header(highlight = "zenburn")

print("this!")
```

```{r}
print("this!")

```